/**
 * Feather "repeat" — https://feathericons.com/?query=repeat
 * Raster/vector asset: `/feather-repeat.svg` (stroke `#F97316`).
 */
export function FeatherRepeat({ className }: { className?: string }) {
  return (
    <img
      src="/feather-repeat.svg"
      alt=""
      className={className}
      width={24}
      height={24}
      decoding="async"
      aria-hidden
    />
  );
}
